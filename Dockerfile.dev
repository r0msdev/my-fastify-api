FROM node:22-alpine

WORKDIR /app

# Layer cache: install deps first, source files later
COPY package*.json ./
RUN npm ci

# Copy source (overridden by volume mount in dev)
COPY . .

EXPOSE 3000

# Sync schema then start with hot-reload
CMD ["sh", "-c", "npm run db:schema:update && npm run dev"]
